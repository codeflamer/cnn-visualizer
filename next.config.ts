import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  images: {
    domains: ["img.freepik.com"], // Add all allowed hostnames here
  },
};

export default nextConfig;
